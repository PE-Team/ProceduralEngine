#version 330 core

layout (location = 0) in vec2 position;
layout (location = 1) in vec2 borderVec;
layout (location = 2) in float isTopValue;

out VS_OUT {
	vec2 border;
	float isTop;
} vs_out;

uniform mat3 transformation;
uniform mat4 projection;

vec4 finalVertex(vec2 vec){
	return projection * vec4(transformation * vec3(vec, 1.0), 1.0);
}

void main() {
	gl_Position = finalVertex(position);
	
	vs_out.border = borderVec;
	vs_out.isTop = isTopValue;
}